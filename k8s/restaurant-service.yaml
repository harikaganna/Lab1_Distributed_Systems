apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
  namespace: yelp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      containers:
        - name: restaurant-service
          image: REPLACE_ECR_URL/yelp/restaurant-service:latest
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: yelp-config
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: yelp-secret
                  key: SECRET_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: yelp
spec:
  selector:
    app: restaurant-service
  ports:
    - port: 8002
      targetPort: 8002
